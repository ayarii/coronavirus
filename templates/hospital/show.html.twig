{% extends 'base.html.twig' %}

{% block title %}Hospital{% endblock %}

{% block body %}
    <h1>Hospital</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hospital.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hospital.name }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ hospital.capacity }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ hospital.adress }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hospital_index') }}">back to list</a>

    <a href="{{ path('hospital_edit', {'id': hospital.id}) }}">edit</a>

    {{ include('hospital/_delete_form.html.twig') }}
{% endblock %}
