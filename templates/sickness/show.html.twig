{% extends 'base.html.twig' %}

{% block title %}Sickness{% endblock %}

{% block body %}
    <h1>Sickness</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sickness.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sickness.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sickness_index') }}">back to list</a>

    <a href="{{ path('sickness_edit', {'id': sickness.id}) }}">edit</a>

    {{ include('sickness/_delete_form.html.twig') }}
{% endblock %}
